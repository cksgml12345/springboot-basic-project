spring:
  application:
    name: portfolio
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:portfolio;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

server:
  port: 8080
